# Новый backend: результат деплоя 2026-09-05

## Что запущено

- Сервер разработки Timeweb Cloud: `185.39.206.18`, Ubuntu 24.04.
- Старый сервер `5.42.97.122` не используется и не является резервным стендом.
- Репозиторий на сервере: `/opt/clothing-store`, базовый коммит `cd1d161`.
- Поверх коммита передан исправленный `medusa-prototype/apps/backend/Dockerfile`:
  перед `update-ca-certificates` устанавливается пакет `ca-certificates`.
- Docker Compose: Medusa, PostgreSQL 15, Redis 7, Caddy, ежедневный `pg_backup`.
- API: <https://api.mariomikke.shop>; `/health` проверен снаружи, ответ `200 OK`.
- Админка: <https://api.mariomikke.shop/app>.
- TLS завершается на балансировщике Timeweb, запросы идут на HTTP-порт 80
  сервера, затем через Caddy к Medusa на порту 9000. Cloudflare не используется.
- SSH по ключу проверен после отключения паролей. Настройки в
  `/etc/ssh/sshd_config.d/00-mario-mikke.conf` имеют приоритет перед cloud-init.
- UFW разрешает 22 и 80, блокирует внешний 9000. PostgreSQL и Redis слушают
  только `127.0.0.1`.

## База и медиа

Это новая база, а не восстановление старого сервера. В репозитории резервных
копий старой базы и загруженных файлов не обнаружено.

- Применены миграции и импортирован демонстрационный каталог из кода: 12 товаров.
- Созданы исходные разделы главной, включая 3 hero-слайда.
- Остатки демонстрационные, не подтверждённые остатки магазинов.
- Проверены Store API и все 36 уникальных изображений демокаталога.
- Проверены вход администратора, Admin API, загрузка PNG и чтение загруженного
  файла через публичный HTTPS. Тестовый файл после проверки удалён.
- Загрузки хранятся в `/opt/clothing-store/medusa-prototype/static`, подключённом
  к `/app/static` контейнера. Файлы не следует хранить только внутри контейнера.
- Пароли и секреты backend: `medusa-prototype/.env.production` на сервере.
- Свежая копия после заполнения базы:
  `medusa-prototype/backups/medusa_20260905_075649.dump.gz` на сервере.
- Автоматические копии сейчас находятся на том же сервере. Отдельное внешнее
  резервное хранение базы И медиа ещё нужно настроить.

## Вход в админку

Два уровня входа:

1. Запрос браузера Basic Auth: пользователь `admin`.
2. Форма Medusa: email `dnptr31@gmail.com`.

Новые пароли хранятся в `/root/mario-mikke-admin.env` на сервере с правами `600`.
Не добавлять этот файл, его содержимое или `.env.production` в Git.
Скачивание паролей в локальный `.env.server-admin.local` требует согласия
пользователя. Восстановление пароля по почте пока не настроено.

## Подключение витрины

Локальная `.env.local` обновлена. Новые публичные значения отдельно сохранены
в `.env.server-storefront.local` в корне проекта, файл исключён из Git.
Backend разрешает Store/Auth запросы с `https://www.mariomikke.shop` и
`http://localhost:3000`; админка работает на домене API.

В Vercel нужно обновить переменные соответствующего проекта:

- `NEXT_PUBLIC_MEDUSA_PUBLISHABLE_KEY` и `NEXT_PUBLIC_MEDUSA_REGION_ID`.
- `MEDUSA_PUBLISHABLE_KEY` и `MEDUSA_REGION_ID`, если используются серверные
  переопределения. Они не должны указывать на старую базу.
- Оба URL backend должны указывать на `https://api.mariomikke.shop`.
- Режим данных должен оставаться `medusa`.

Значения взять из `.env.server-storefront.local`. Затем выполнить Redeploy:
`NEXT_PUBLIC_*` встраиваются в клиентскую сборку. Доступа к Vercel в этой
сессии нет, изменения там не выполнены.

Старые идентификаторы товаров, корзин и регионов больше не существуют в новой
базе. При проверке использовать новую корзину; ранее сохранённые корзины и
избранное нельзя считать восстановленными.

## Следующие шаги

1. Обновить Vercel и проверить витрину, корзину и избранное на новой базе.
2. Проверить работу заказчика в админке, повторно внести утраченный контент
   либо восстановить его из отдельной копии, если она найдётся.
3. Настроить внешние резервные копии БД и медиа, провести пробное восстановление.
4. Отдельно подключить секреты Т-Банка, СДЭК, SMTP и уведомлений к Docker Compose.
   На этом сервере они ещё не настроены; реальные оплаты и отправки не проверялись.
5. Уточнить у провайдеров разрешение нового исходящего IP `185.39.206.18`,
   если используется IP-фильтрация. IP балансировщика для этого не подходит.
6. Провести тесты DEMO-оплаты и доставки, затем продолжить бизнес-функции
   по основному плану запуска. Работающий стенд не означает готовность к продажам.
