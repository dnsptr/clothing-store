import Link from "next/link";
import styles from "./account.module.css";

/**
 * Личный кабинет ещё не реализован.
 *
 * Здесь была форма входа по коду из сообщения. Кода никто не отправлял:
 * запросов к `/auth/customer` в проекте нет вообще, а подтверждение состояло в
 * проверке `code.length >= 4` — любые четыре цифры приводили к экрану «Вы вошли
 * в личный кабинет». Сессии при этом не возникало, история заказов не
 * загружалась, а телефон и e-mail покупателя собирались и никуда не
 * сохранялись.
 *
 * Форма вернётся вместе с настоящей аутентификацией Medusa: реальная отправка
 * кода, ограничение частоты запросов, срок жизни и лимит попыток.
 */
export default function AccountClient() {
  return (
    <main className={styles.page}>
      <section className={styles.loginZone} aria-label="Личный кабинет">
        <div className={styles.authPanel}>
          <h1>Личный кабинет скоро откроется</h1>
          <p className={styles.helperText}>
            Мы готовим вход, историю заказов и сохранённые адреса. Пока оформить
            заказ можно без регистрации — все данные для доставки запрашиваются
            при оформлении.
          </p>
          <Link href="/catalog" className={styles.catalogLink}>
            Перейти в каталог
          </Link>
        </div>
      </section>
    </main>
  );
}
